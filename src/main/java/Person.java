/** Una clase es una plantilla para la creación de objetos.
 * <p>
 * Está conformada por atributos (variables) y métodos (funciones).
 * <p>
 * Los atributos y métodos son llamados los miembros de la clase.
 * <p>
 * La clase no necesariamente debe ser pública, pero en caso de tener 2 o más clases en un archivo
 * al menos UNA debe ser pública.
 */
public class Person {

    // Declaración de la variable (atributo).
    String name;

    // Método de la clase.
    public String getName() {
        return name;
    }
}

class Animal{

}